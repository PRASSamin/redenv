# @redenv/core

This package provides the core cryptographic functions and shared utilities for the Redenv secret management system.

## Internal Package

This is an internal helper package designed to be used by other official Redenv packages (`@redenv/cli`, `@redenv/client`). It is not intended for direct public consumption. Its API may change without notice in accordance with the needs of the Redenv ecosystem.
